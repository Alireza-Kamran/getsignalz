# Secrets — never commit
config.py
.env
.claude_api_key

# Runtime state
state.json
journal.json
bot.log
*.log
*.pid
.review_msg_id
.signal_count
.night_report.json
# Liveness timestamp for the cross-restart downtime detector (live.py)
.heartbeat
# Nightly-session success marker + lock, read by self_improve.sh's retries
.last_session
.review_latch
.self_improve.lock
.crontab.bak
# Per-strategy runtime journals
journal_s2.json

# Pre-reset state backups (kept locally, not part of the repo)
journal_pre_reset_*.json
state_pre_reset_*.json

# Generated backtest reports — regenerate with s2_report.py
s2_backtest_trades.csv
s2_backtest_monthly.txt


# Python artifacts
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# Old naming (renamed to ai_brain.py)
claude_brain.py

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# dated safety backups from nightly self-learn sessions
*.bak_*

# io_safe keeps one .bak generation beside every file it writes
*.bak

{
  "version": 1,
  "last_updated": "2026-07-28",
  "updated_by": "auto-nightly",
  "min_score": 7,
  "max_trades": 1,
  "risk_pct": 0.01,
  "trail_pct": 0.08,
  "tp_ratio": 2.0,
  "session_start_utc": 11,
  "session_end_utc": 24,
  "min_adx": 30,
  "watchlist": [
    "ETH",
    "WLD",
    "ARB",
    "SUI",
    "SOL",
    "INJ",
    "AAVE",
    "AVAX",
    "ATOM",
    "OP",
    "APT",
    "DOGE"
  ],
  "signal_weights": {
    "base_zone_target": 2,
    "zone_reliability_good": 2,
    "zone_reliability_neutral": 1,
    "zone_reliability_poor": 0,
    "ob_fvg_confluence": 2,
    "timeframe_4h_or_1d": 2,
    "timeframe_1h": 1,
    "timeframe_15m": 0
  },
  "notes": "2026-07-28 nightly: no S1 parameter changed (engine is off). Work tonight was in live.py/executor.py. (1) BUG FIX, the important one: _check_closed recorded the exit price as get_price(coin) -- the current mid at the moment the bot noticed the position was gone, up to POLL=20s stale. The first live S2 trade (ARB long, 07-27) actually filled its stop at 0.07817 per Hyperliquid user_fills, but the mid 22s later was 0.07767, so the journal, the owner DM and the public channel post all recorded -19.4% for a trade that really lost -13.1% -- a 48% overstatement, biased worst on losses because price keeps running after a stop. Added executor.get_close_fill() (size-weighted average of actual closing fills since the trade opened, None if unattributable) and live._exit_price() which falls back to the old mid behaviour on any failure. Verified against the real ARB fill. Corrected the stored ARB record in journal.json/state.json/journal_s2.json (exit 0.07817, lev_pct -13.13, pnl_usd -5.83, rr -0.870); balance-derived stats (max_drawdown 1.58%) were already correct, which is itself confirmation the bug was narrowly the exit price. (2) S2 scan moved ABOVE the in_session gate: S2 was validated 24/7 but only ran 11-24 UTC, giving 0.39 signals/day vs the >=1-per-2-days product requirement that is the entire reason S2 exists. Now 0.53/day. Honest caveat: the newly enabled hours are the WEAKEST block (n=30, WR 56.7%, EV +0.199%/trade, t=0.99 -- positive but statistically indistinguishable from zero) vs 11-24 (n=80, WR 67.5%, EV +0.655%, t=4.43). This buys frequency at near-breakeven EV; it does not improve edge. (3) Position management (_check_closed/_check_trail/_check_trail_s2) moved above the quiet-hours gate and its sleep(600) dropped to sleep(POLL): Mr G's rule pauses SCANNING, not position management, and S2 exits exclusively via the ratchet, so it was frozen 02-04 nightly. No S2 parameter touched -- stretch/ADX/RSI-depth buckets over 110 backtested trades show no actionable gradient (the 4.0+ ATR stretch bucket the live ARB loss came from is in fact the BEST one, +0.818%/trade, so the loss does not indict the entry filter). One live trade is not evidence; it lost exactly its 1R budget (-0.98% of account vs 1.0% risked), so sizing is correct.",
  "suspended_coins": {},
  "engine": "S1_DISABLED \u2014 live engine is strategy2.py (mean reversion). Every parameter in this file (min_score, min_adx, trail_pct, tp_ratio, session_*, watchlist, signal_weights) belongs to the RETIRED liquidity-pool engine and has NO effect while S1_ENABLED=False in live.py. S2's parameters live in strategy2.py."
}
{
  "version": 1,
  "last_updated": "2026-09-05",
  "updated_by": "auto-nightly",
  "min_score": 7,
  "max_trades": 1,
  "risk_pct": 0.01,
  "trail_pct": 0.08,
  "tp_ratio": 2.0,
  "session_start_utc": 11,
  "session_end_utc": 24,
  "min_adx": 30,
  "watchlist": [
    "ETH",
    "WLD",
    "ARB",
    "SUI",
    "SOL",
    "INJ",
    "AAVE",
    "AVAX",
    "ATOM",
    "OP",
    "APT",
    "DOGE"
  ],
  "signal_weights": {
    "base_zone_target": 2,
    "zone_reliability_good": 2,
    "zone_reliability_neutral": 1,
    "zone_reliability_poor": 0,
    "ob_fvg_confluence": 2,
    "timeframe_4h_or_1d": 2,
    "timeframe_1h": 1,
    "timeframe_15m": 0
  },
  "notes": "S1 disabled (engine=strategy2.py). 2026-09-05 session: NO STRATEGY PARAMETER CHANGES. THE FINDING: THE REPORT WAS BLIND TO 8 OF THE 20 COINS IT TRADES. live.py's scan loop iterated `WATCHLIST` imported from trader.py -- the RETIRED S1 list of 12 -- while the engine trades strategy2.WATCHLIST (20). ADA, BNB, BTC, FIL, LDO, NEAR, TIA and XLM have therefore NEVER printed an RSI/ADX line, so every section mined from the scan log was computed on a sample selected by a dead engine's watchlist. Consequences, measured not assumed: (1) REALISED R BY FEED QUALITY silently dropped 6 of 19 closed trades -- 32% of the book, 5 of them losses, meanR -0.717, INCLUDING ALL FOUR BTC TRADES, the worst record we have -- because `stale.get(coin)` returns None for an unlogged coin and the trade just fell out of `paired` with no counter. The discarded cohort is WORSE than either published bucket, so the headline 'clean +1.599R vs dirty -0.388R' is part real effect and part selection artifact. (2) FEED HEALTH could not see NEAR, whose candles went stale on 09-04 (two signals an hour apart with byte-identical rsi=78.0 adx=22.7 stretch=5.03 while its mid moved 2.5%). That staleness was caught by an EXCHANGE REJECTION at 21:01, not by the table built to catch exactly it. (3) ENTRY GATE CENSUS n=9823 is a 12-coin sample described as a census of 20. FIXED (live.py): a supplementary observability pass logs RSI/ADX/real_close for the 8 uncovered coins, placed BELOW the S2 entry decision so it adds zero signal-to-fill drift (the FIL failure mode), and computing indicators inline rather than via strategy2.build_df -- which returns None when its freshness guard trips, so routing through it would emit no row for precisely the coins whose feeds are worst. Measured cost ~13s per candle, in the loop's idle hour. FIXED (analyze.py): _feed_quality_split extracted as a pure helper that RETURNS the leftovers; the report now prints UNMEASURABLE n/meanR/coin names and the % of book excluded; coverage is stated by NAME with the traded coins called out; the census docstring no longer claims 'all 20 coins'. SECOND FIX -- A RESTART IS NOT A STALL: _loop_latency counted every re-printed candle header, so the 10 restarts on 09-04 produced NINE phantom stalls of 6-54 min each, all labelled 'SELF-BLOCKED -- venue was healthy, we were not looking', inflating 'OURS to prevent' to 494 min against a true 121. Now keeps only the FIRST arrival per candle hour (which is what the function documents), and reports restarts separately as restarts; a genuinely late first arrival (2026-08-22 17:50, post-blackout) is still flagged, pinned by test. Also corrected the heading: since 09-02 position management runs ABOVE the review, so a late candle delays ENTRIES, not the ratchet. ADDED: test_scan_coverage.py, 45 assertions (behavioural + log-format round-trip + source-order proving observability sits below the entry call and below the ratchet). DID NOT CHANGE: every S2 constant (owner-locked; EV +0.2405R at n=19 but t=+0.67, indistinguishable from a coin flip). Did not point the main scan at strategy2.WATCHLIST -- that would move _mark_scan_ok(), whose liveness semantics were only stabilised on 08-31 after a 3-of-4 false-alarm run. Did not run apply_config_to_trader() (no S1 value moved).",
  "suspended_coins": {},
  "engine": "S1_DISABLED \u2014 live engine is strategy2.py (mean reversion). Every parameter in this file (min_score, min_adx, trail_pct, tp_ratio, session_*, watchlist, signal_weights) belongs to the RETIRED liquidity-pool engine and has NO effect while S1_ENABLED=False in live.py. S2's parameters live in strategy2.py."
}
{
  "version": 1,
  "last_updated": "2026-08-04",
  "updated_by": "auto-nightly",
  "min_score": 7,
  "max_trades": 1,
  "risk_pct": 0.01,
  "trail_pct": 0.08,
  "tp_ratio": 2.0,
  "session_start_utc": 11,
  "session_end_utc": 24,
  "min_adx": 30,
  "watchlist": [
    "ETH",
    "WLD",
    "ARB",
    "SUI",
    "SOL",
    "INJ",
    "AAVE",
    "AVAX",
    "ATOM",
    "OP",
    "APT",
    "DOGE"
  ],
  "signal_weights": {
    "base_zone_target": 2,
    "zone_reliability_good": 2,
    "zone_reliability_neutral": 1,
    "zone_reliability_poor": 0,
    "ob_fvg_confluence": 2,
    "timeframe_4h_or_1d": 2,
    "timeframe_1h": 1,
    "timeframe_15m": 0
  },
  "notes": "2026-08-04 nightly: TRAIL_START_R reverted 0.75 -> 1.0 (strategy2.py), because the model that chose 0.75 on 08-02 is one live cannot reproduce. portfolio2.simulate drives the ratchet off the bar CLOSE and does not test the freshly-raised stop until the NEXT bar -- its docstring called that 'the conservative proxy for a level held long enough for a 20s poll', which is conservative about WHEN the stop arms and silently optimistic about whether it SURVIVES, granting every raised stop a full bar of immunity no real stop has. live._check_trail_s2 polls the mid every ~20s and places the stop AT the market the instant price prints the level. FIRST LIVE PROOF: BTC 2026-08-03 armed at +0.75R at 13:49:56 and filled 22s later at +0.706R. NEW TOOL sweep_trail_mode.py reports both achievable bounds and asserts equivalence with portfolio2 at mode='close' (printed OK). 20 coins, 5000 bars, real prices, fees, cap 2: close n=121 WR 57.0% net +42.49% EV +0.351% t=2.62 ex-top5 +18.56% (>=1.5R:19, >=3R:8); touch_opt n=122 WR 58.2% +34.65% EV +0.284% t=2.45 (15, 7); touch_pess n=122 WR 59.0% +8.74% EV +0.072% t=0.83 ex-top5 -2.81% (7, 2). The close row sits ABOVE the optimistic bound, i.e. outside the range live can occupy at all. Signature: win rate goes UP as EV collapses -- placing the stop on the price rescues near-misses and truncates runners, and this strategy's entire EV is in the runners. Under both achievable models the 0.75-vs-1.00 ordering INVERTS and is monotone: net acct % at tsr 0.50/0.75/1.00/1.25/1.50 = close 30.07/42.49/40.92/40.20/40.70, touch_opt 33.51/34.65/40.45/49.26/52.61, touch_pess 12.26/8.74/14.54/23.98/25.17 -- 0.75 is a local MINIMUM under pess. Chose 1.00 not 1.25/1.50 because the curve runs monotone to the edge of the swept range and picking a monotone boundary is a mechanical fact not a measured edge (same reasoning that kept TRAIL_STEP_R 0.25 and TP_R off 'none'); 1.00 is also not newly fitted, it is what ran until 08-02, so this reverts a bad-evidence change rather than fitting a fresh one. Rejection tests vs 0.75 under BOTH achievable models: cap2 (+39.71/+14.54 vs +33.91/+8.74), cap4 (+40.55/+12.74 vs +35.00/+7.93), alphabetical ranking (+38.67/+13.50 vs +33.12/+7.70); positive in BOTH out-of-sample halves under both models where 0.75 is NEGATIVE in-sample under pess (-2.47%); and 1.00 is the only setting whose ex-top5 is non-negative under pess (+0.54% vs -2.81%), speaking to the known tail-dependence. RECORDED HONESTLY: at concurrency cap 1 the two TIE (+22.80/+5.16 vs +22.78/+5.71), 0.75 marginally ahead on the pess bound; cap 1 is not deployed (MAX_TRADES=2). COST, deliberate: the 15.3% of trades peaking between 0.5R and 1.0R go back to full stops and WR drops ~5pp (pess 59.0 -> 53.7) while EV rises (+0.072 -> +0.120) -- lower win rate, higher EV, taken on purpose. A per-rung 'gap' forbidding the stop from resting within X R of price was BUILT AND MEASURED as the direct mechanism fix, then REJECTED as non-monotonic noise (pess at tsr 0.75: +8.74/+4.56/+3.11/+1.03/+13.09 across gap 0->0.5). TRAIL_STEP_R re-checked at tsr=1.00 under both new models: 0.25 still beats 0.50, unchanged. LIVE CORROBORATION n=5: best trade is +0.99R, ZERO above 1.5R, all three winners pinned at the arming level (+0.71R, +0.98R, +0.99R), meanR +0.156, winners avg 4.5h vs losers 17.2h. ALSO FIXED: (1) analyze.full_report classified wins as result=='tp', unreachable under S2 since the ratchet cancels the TP and every trade exits via the stop -- every coin, score band and factor read WR 0% while P&L was positive (AVAX showed WR:0% AvgPnL:+15.7%), and the standing coin-removal bar is '<30% WR AND negative P&L over 5+ trades', so the WR half was stuck at 0 for everything. Now classifies on lev_pct>0. (2) Added an R-MULTIPLE DISTRIBUTION section to the report, in R not leveraged % (the exchange leverage clamp distorts the published number). (3) tg.note_channel_failure: the channel went unreachable 2026-08-03 22:01 UTC ('@GetSignalz' -> 'chat not found', confirmed via getChat under every casing; bot token itself is fine, owner DM works) and produced 194 identical bot.log lines in 4h with nothing escalated -- tg.send was silently returning None on rejection. Now DMs the owner once and suppresses the spam. NEEDS MR G: only he can restore the channel name or re-add the bot. (4) test_ratchet case 2b hard-coded a 0.9R peak, silently encoding TRAIL_START_R<0.9; now derived from the constants. 10/10 pass. NOT CHANGED: MAX_ADX 25, RSI 25/75, stretch 1.5, TP_R 3.0, TRAIL_STEP_R 0.25, MAX_TRADES 2, risk 1%, watchlist. Every field in this file other than these notes still belongs to the DISABLED S1 engine.",
  "suspended_coins": {},
  "engine": "S1_DISABLED \u2014 live engine is strategy2.py (mean reversion). Every parameter in this file (min_score, min_adx, trail_pct, tp_ratio, session_*, watchlist, signal_weights) belongs to the RETIRED liquidity-pool engine and has NO effect while S1_ENABLED=False in live.py. S2's parameters live in strategy2.py."
}
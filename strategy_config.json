{
  "version": 1,
  "last_updated": "2026-09-02",
  "updated_by": "auto-nightly",
  "min_score": 7,
  "max_trades": 1,
  "risk_pct": 0.01,
  "trail_pct": 0.08,
  "tp_ratio": 2.0,
  "session_start_utc": 11,
  "session_end_utc": 24,
  "min_adx": 30,
  "watchlist": [
    "ETH",
    "WLD",
    "ARB",
    "SUI",
    "SOL",
    "INJ",
    "AAVE",
    "AVAX",
    "ATOM",
    "OP",
    "APT",
    "DOGE"
  ],
  "signal_weights": {
    "base_zone_target": 2,
    "zone_reliability_good": 2,
    "zone_reliability_neutral": 1,
    "zone_reliability_poor": 0,
    "ob_fvg_confluence": 2,
    "timeframe_4h_or_1d": 2,
    "timeframe_1h": 1,
    "timeframe_15m": 0
  },
  "notes": "S1 disabled (engine=strategy2.py). 2026-09-02 session: NO STRATEGY PARAMETER CHANGES. Book flat since ETH closed 08-25 09:52 (8 days); n=17 unchanged (WR 41.2%, sumR +2.61, avgWin +1.772R, avgLoss -0.979R, EV +0.153R, breakeven WR 35.6% vs actual 41.2%, t=+0.42 -- positive sign, no significance). Tonight's finding was structural, not statistical: nightly_review() ran ai_brain and _self_improve() INLINE in live.py's main loop and sat ABOVE position management, so _check_trail_s2 (the ratchet -- the only mechanism in this system that produces profit) did not execute while it ran. Measured over 41 nights the 23:00 candle started a median 21s late but p90 19.4 min, worst 27.7 min, 14 nights over 5 min; every other hour sits at ~10s median. 11 review-nights crossed an open position = 70 min of frozen ratchet (63 of them the ETH SHORT of 08-21..08-25). Ceiling is far higher than the observed worst case: _beat(4800) tolerates 80 min and BRAIN_TIMEOUT is 3600s. FIXED by moving version_push / nightly_review / weekly_review BELOW _check_closed and _check_trail_s2. Paired fix in review.py: should_nightly_review was minute==0 EXACTLY and the reorder puts three more Hyperliquid round-trips ahead of it, so the window was widened to minute<10 (weekly to >=30) -- safe because live.py sets the once-per-period latch BEFORE the call. Pinned by test_review_order.py, 50 assertions, with a negative control confirming 4/4 ordering assertions fail on the pre-fix source. NOT fixed, deliberately: the candle SCAN still sits below the review, so entries on the 23:00 candle can still be up to ~20 min late (live-exercised once: SOL 08-16 23:09:47, +2.48R). Moving it would invalidate SCAN_STALE_ALERT_SEC=8100, which is sized to clear exactly this review, and that detector was only stabilised on 08-31 after a 3-of-4 false-alarm run. Reported to Kamran instead.",
  "suspended_coins": {},
  "engine": "S1_DISABLED \u2014 live engine is strategy2.py (mean reversion). Every parameter in this file (min_score, min_adx, trail_pct, tp_ratio, session_*, watchlist, signal_weights) belongs to the RETIRED liquidity-pool engine and has NO effect while S1_ENABLED=False in live.py. S2's parameters live in strategy2.py."
}
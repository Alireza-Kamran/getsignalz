{
  "version": 1,
  "last_updated": "2026-09-04",
  "updated_by": "auto-nightly",
  "min_score": 7,
  "max_trades": 1,
  "risk_pct": 0.01,
  "trail_pct": 0.08,
  "tp_ratio": 2.0,
  "session_start_utc": 11,
  "session_end_utc": 24,
  "min_adx": 30,
  "watchlist": [
    "ETH",
    "WLD",
    "ARB",
    "SUI",
    "SOL",
    "INJ",
    "AAVE",
    "AVAX",
    "ATOM",
    "OP",
    "APT",
    "DOGE"
  ],
  "signal_weights": {
    "base_zone_target": 2,
    "zone_reliability_good": 2,
    "zone_reliability_neutral": 1,
    "zone_reliability_poor": 0,
    "ob_fvg_confluence": 2,
    "timeframe_4h_or_1d": 2,
    "timeframe_1h": 1,
    "timeframe_15m": 0
  },
  "notes": "S1 disabled (engine=strategy2.py). 2026-09-04 session: NO STRATEGY PARAMETER CHANGES. THE FINDING: A STOP THAT FILLED SHORT LEFT A TRADE IMMORTAL. BTC 2026-09-03 SHORT stopped out at 19:21:24 and the bot did not notice for 6.7 hours. Hyperliquid's 'Stop Market' is a stop-limit with a ~0.3% band filled IOC; the resting book inside that band was thinner than the order, so it swept five levels (83472 -> 83514) for 0.00473 of 0.00486 and left 0.00013 ($10.74) resting. executor.get_positions() reports any coin with `sz != 0`, and live._check_closed() closes a trade only when the coin is ABSENT from that dict -- so a residue is neither zero nor a position and falls through both. Confirmed against real candles: BTC's real high was 83750 at 21:00 vs a stop at 83456.68, breached for four consecutive hours (19:00-22:00). All 18 prior closes filled to EXACTLY zero, which is why `sz != 0` had never been wrong. COST: a MAX_TRADES slot held (effective MAX_TRADES=1 for 6.7h, and this was the first-ever concurrent pair), a -1.118R loss missing from every published statistic, and an unprotected residue -- while the report described a $400 position protected at -1.00R. THE SECOND, WORSE HALF: live.py's restore path sets size = abs(hl['size']) and writes it back to state.json, so ONE restart would have laundered the residue into 'the size we opened' (0.00486 -> 0.00013), making the ratio 1.00 and the residue undetectable forever. Same disease the repo already cured for the stop (the ratchet overwrote sl, hence sl_orig); size needed the identical treatment. FIXED: tracker.register_position + both live.py open sites now record an immutable size_orig; the restore path reads it BEFORE size is overwritten and persists it (with a one-time fallback to size, which is only correct on the first restart after a partial close); new live._reconcile_dust() runs ABOVE _check_closed (absence from `positions` is the only signal _check_closed reads) and classifies by FRACTION OF SIZE OPENED, not by notional -- $10 is dust on BTC and a whole position on DOGE. <=10% residue: flatten and record the close (exit price already correct, get_close_fill size-weights all closing fills). 10-90%: a real position with no stop under it -- logged at error and DMed, deliberately NOT auto-resolved, because re-arming a stop on a leg the bot cannot price is a trading decision. ADDED: test_dust_close.py, 40 assertions (behavioural + source-order + an explicit regression on the size_orig laundering trap), redirecting close_trade/tg and verifying the redirect held. ADDED: analyze.py OPEN BOOK now asserts MAE >= -1.00R -- the mirror of the existing locked-vs-MFE invariant, on the loss side. The closed-trade invariants all PASSED tonight because the trade was never recorded as closed; the open book had no invariant at all. It now back-derives the breach price and prints 83750.15, matching the real candle high of 83750 exactly. VERIFIED FROM LAST NIGHT: the pinned dashboard is ALIVE -- 0 dashboard errors in 711 log lines since the 09-03 02:09 restart (176 before it), actively editing msg 218. The review ran exactly once at 23:00:09-23:02:15. No new SELF-BLOCKED latency row. DID NOT CHANGE: every S2 constant (owner-locked; t=+0.83 at n=18 justifies nothing). Did not hand-edit state.json or poke the exchange manually -- the fix self-heals BTC on restart through the bot's own code path, which exercises and proves it. Did not run apply_config_to_trader() (no S1 value moved; it rewrites trader.py for nothing).",
  "suspended_coins": {},
  "engine": "S1_DISABLED \u2014 live engine is strategy2.py (mean reversion). Every parameter in this file (min_score, min_adx, trail_pct, tp_ratio, session_*, watchlist, signal_weights) belongs to the RETIRED liquidity-pool engine and has NO effect while S1_ENABLED=False in live.py. S2's parameters live in strategy2.py."
}
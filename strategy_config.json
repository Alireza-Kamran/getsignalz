{
  "version": 1,
  "last_updated": "2026-07-12",
  "updated_by": "auto-nightly",
  "min_score": 6,
  "max_trades": 1,
  "risk_pct": 0.01,
  "trail_pct": 0.08,
  "tp_ratio": 2.0,
  "session_start_utc": 11,
  "session_end_utc": 24,
  "min_adx": 30,
  "watchlist": [
    "ETH",
    "WLD",
    "ARB",
    "SUI",
    "SOL",
    "INJ",
    "AAVE",
    "AVAX",
    "ATOM",
    "OP",
    "RUNE",
    "BNB",
    "APT",
    "DOGE"
  ],
  "signal_weights": {
    "ema_cloud": 2,
    "entry_conservative": 2,
    "entry_aggressive": -99,
    "macro_4h_aligned": 2,
    "macro_4h_neutral": 1,
    "macro_4h_against": -1,
    "adx_strong": 1,
    "cloud_wide": 1,
    "willr_exhausted": -99
  },
  "notes": "2026-07-12 nightly: VERIFICATION NIGHT, no parameter changes. Zero new CM trades since the 2026-07-10 conservative-only gate went live (23:05 UTC) - confirmed ZERO signals fired at all during the full Jul 11 session window (11:00-24:00 UTC), not just zero closes. Expected given historical conservative-entry rate (~2/21 recent signals -> ~2/week); one day of silence is not yet concerning, but if 5-7 session days pass with zero signals the gate may be too strict and should be revisited (e.g. relax to allow aggressive entries only when conservative is absent AND ADX/4H alignment is exceptionally strong - not attempted yet, no evidence either way). Verified trader.py still has has_conservative hard gate + MIN_ADX=30 intact after last nights separate ai_brain.py v1.12.0 run (which only touched executor.py/ai_brain.py, not trader.py). That run independently fixed the exact ETH SL-slippage bug flagged in this file on 2026-07-10: executor.py slippage_buf 0.02->0.003, closing the tail-risk gap that let a 1.29% SL cap execute at 65.8% leveraged loss instead of ~26%. Acknowledged, not re-touched. bot.log clean otherwise: only transient HL 502s (retried, recovered) and one restart-overlap already-running line at 23:03:32 (expected pattern). One anomaly: unexplained ~40min gap + silent restart at 00:41:27 UTC Jul 11 (last log line 00:01:08 off-session, next line is a fresh ONLINE banner, no error logged in between) - off-session, no open positions, no harm, but no journalctl access this session to find root cause; flag for next session if it recurs. Bot currently FLAT (no open positions). current_drawdown 82.1% is essentially AT the max_drawdown 82.57% high-water mark - the account is sitting at its worst point, entirely inherited from the pre-gate aggressive-entry losses (-198% total). With aggressive entries now gated out and the slippage tail-risk fixed, expect drawdown to stabilize; watch for confirmation over the next few conservative-entry closes rather than assuming it yet.",
  "suspended_coins": {}
}
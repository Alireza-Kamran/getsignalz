{
  "version": 1,
  "last_updated": "2026-09-03",
  "updated_by": "auto-nightly",
  "min_score": 7,
  "max_trades": 1,
  "risk_pct": 0.01,
  "trail_pct": 0.08,
  "tp_ratio": 2.0,
  "session_start_utc": 11,
  "session_end_utc": 24,
  "min_adx": 30,
  "watchlist": [
    "ETH",
    "WLD",
    "ARB",
    "SUI",
    "SOL",
    "INJ",
    "AAVE",
    "AVAX",
    "ATOM",
    "OP",
    "APT",
    "DOGE"
  ],
  "signal_weights": {
    "base_zone_target": 2,
    "zone_reliability_good": 2,
    "zone_reliability_neutral": 1,
    "zone_reliability_poor": 0,
    "ob_fvg_confluence": 2,
    "timeframe_4h_or_1d": 2,
    "timeframe_1h": 1,
    "timeframe_15m": 0
  },
  "notes": "S1 disabled (engine=strategy2.py). 2026-09-03 session: NO STRATEGY PARAMETER CHANGES. n=18 (AAVE 09-02 SHORT +2.97R is the first trade in 9 days and the best on record): WR 44.4%, sumR +5.58, avgWin +1.922R, avgLoss -0.979R, EV +0.310R/trade, breakeven WR 33.8% vs actual 44.4% (+10.7pp), t=+0.83 (0.91 sigma). Still |t|<2 -- positive sign, no significance, nothing tunable. TONIGHT'S FINDING WAS ONE BAD DATA ROW BREAKING FOUR INSTRUMENTS. The OP 2026-08-13 record (flagged reconstructed_from_journal, rebuilt by an ad-hoc script after save_state erased it, and never mentioned in the CHANGELOG) differs from all 34 other records in exactly two ways: tz-AWARE timestamps ('+00:00') and NULL excursion fields. That single row was: (1) aborting the AVAILABILITY open-position cross-reference, (2) killing the ENTIRE LOOP LATENCY section -- added the previous night specifically to answer this night's primary question -- because _open_during did .replace('Z','') which strips a Z but not an offset, so fromisoformat returned an aware datetime and any comparison raised TypeError; (3) printing a FALSE 'INVARIANT VIOLATED' on a textbook stop-out, because float(None or 0.0) fabricated MFE/MAE of exactly 0.00 against a real -1.09R exit; and (4) HOLDING THE OWNER'S PINNED TELEGRAM DASHBOARD DEAD SINCE 2026-09-02 12:02 -- tracker.py:295 used t.get(k, 0) which returns None when the key EXISTS with a null value, so the default never fired, None<0 raised, and update_dashboard's blanket except swallowed it into ~800 identical log lines with no traceback. FIXED: analyze._naive_utc() (strip the offset, never trust it; unparseable -> skip the row, never abort the section); analyze._maybe_r() (null excursion stays None, excluded from every excursion statistic but the trade stays VISIBLE with realised R and prints 'n/a' -- dropping it would recreate the under-count the coverage warning exists to catch); tracker.py null-safe drawdown filter + the error handler now prints file:line and exception type. ADDED: _stall_cause() -- LOOP LATENCY now separates 'venue down' from 'SELF-BLOCKED'. These are opposite risks and the section printed both as 'ratchet frozen'. Restored latency immediately found a NEW freeze invisible last night: 2026-09-02 07:00 AAVE SHORT frozen 27.9 min, but 311 Hyperliquid 502s inside the window -- venue down, stop already resting on the exchange, not ours to fix. Book total is now 142 min frozen of which 114 min was OURS (the four review nights). ADDED: test_null_record.py, 38 assertions, shaped around the DATA not the functions. Negative control run and recorded in memory rather than kept as a file: all four pre-fix expressions confirmed to raise/fabricate. VERIFIED FROM LAST NIGHT: the review ran EXACTLY ONCE on 09-02 (one 'Nightly review starting' at 23:00:04, execv restart at 23:06:18, 'Review latches restored' at 23:06:50, NO second review) -- the .review_latch persistence works and the widened minute<10 window is not re-entering. DID NOT CHANGE: every S2 constant (owner-locked; t=+0.83 justifies nothing). Did not repair the OP row in state.json -- the readers are hardened and tested, while save_state's non-atomic read-modify-write ALREADY erased this exact record once, and editing live state to tidy cosmetics would repeat the original failure. Harden the reader, not the row. Did not run apply_config_to_trader() (no S1 value moved; it rewrites trader.py for nothing).",
  "suspended_coins": {},
  "engine": "S1_DISABLED \u2014 live engine is strategy2.py (mean reversion). Every parameter in this file (min_score, min_adx, trail_pct, tp_ratio, session_*, watchlist, signal_weights) belongs to the RETIRED liquidity-pool engine and has NO effect while S1_ENABLED=False in live.py. S2's parameters live in strategy2.py."
}
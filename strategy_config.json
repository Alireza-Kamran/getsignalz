{
  "version": 1,
  "last_updated": "2026-09-22",
  "updated_by": "auto-nightly",
  "min_score": 7,
  "max_trades": 1,
  "risk_pct": 0.01,
  "trail_pct": 0.08,
  "tp_ratio": 2.0,
  "session_start_utc": 11,
  "session_end_utc": 24,
  "min_adx": 30,
  "watchlist": [
    "ETH",
    "WLD",
    "ARB",
    "SUI",
    "SOL",
    "INJ",
    "AAVE",
    "AVAX",
    "ATOM",
    "OP",
    "APT",
    "DOGE"
  ],
  "signal_weights": {
    "base_zone_target": 2,
    "zone_reliability_good": 2,
    "zone_reliability_neutral": 1,
    "zone_reliability_poor": 0,
    "ob_fvg_confluence": 2,
    "timeframe_4h_or_1d": 2,
    "timeframe_1h": 1,
    "timeframe_15m": 0
  },
  "notes": "2026-09-22: No S1 value changed - S1 is disabled, tuning it is theatre, so apply_config_to_trader() was NOT run (it rewrites trader.py for nothing). THE FINDING was in the OTHER Claude session. The nightly loop runs two: the OUTER one (self_improve.sh -> claude -p) which SUPERVISION tracks from selflearn.log, and the INNER ai_brain that review.py calls at 23:20, which had NO health record at all - run_ai_brain RETURNS its error into .night_report.json, and version_push DELETES that file after reading it. The inner brain was dead two nights running and no report showed it: 09-20 the CLI refused to start (rc=1, malformed permission rule), and 09-21 it ran for 772 SECONDS, answered in PROSE, and json.loads threw at char 0 - thirteen minutes of analysis over a 37K-token prompt converted into a 300-char error string. The parser was json.loads() behind a fence-stripper that only fired when the reply STARTED with ```. FIXED ai_brain._extract_json: three layers (whole reply / any fenced block, wherever it sits / any balanced {...} span, counting braces OUTSIDE string literals because this schema puts free prose in 'reason'/'summary'). Non-dict JSON is now REJECTED rather than passed into .get() - mutation-checked, the old parser accepted 5/null/[1,2] and reported the schema violation to the owner as \"Brain error: 'int' object has no attribute 'get'\", an internal crash standing in for a format error. The prose is PRESERVED into result['analysis'] (the reasoning is the expensive half and was already paid for) and a short repair pass re-expresses it as the schema. ADDED analyze BRAIN HEALTH, mined from CHANGELOG.md - the only durable trace - and NOT from a new ledger, which would start empty tonight while a second record that should agree is exactly what drifted for 18 commits. It reports 31 pre-v1.53.4 nights as UNCLASSIFIABLE rather than as successes: that wording was published both for a clean review and for a brain that never ran. EXIT-SIDE, fresh sweep_trail_mode (n=94): TRAIL_START_R=2.50 is best under close/touch_opt and second under touch_pess; 2.00 is WORSE on all three (+46.72/+52.21/+34.66 vs +50.83/+59.74/+42.38), which FALSIFIES the MFE-column intuition that lowering the arm would have rescued ETH 09-03 (+2.05R->-1.26R) and DOGE 09-10 (+1.97R->-1.08R). trail_gap_r=0.25 is still best on both bounds but the margin is now +4.05pp/+0.80pp, NOT the +6.6/+7.4pp carried in the notes since 08-26 - the standing recommendation is weaker than stated. New test_brain_json.py 50/50; full suite 24/24.",
  "suspended_coins": {},
  "engine": "S1_DISABLED \u2014 live engine is strategy2.py (mean reversion). Every parameter in this file (min_score, min_adx, trail_pct, tp_ratio, session_*, watchlist, signal_weights) belongs to the RETIRED liquidity-pool engine and has NO effect while S1_ENABLED=False in live.py. S2's parameters live in strategy2.py."
}